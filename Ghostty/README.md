# Ghostty

## Install

```bash
bash ~/Developer/config/Ghostty/install.sh
```

This repository stores Ghostty config in:

```text
~/.config/ghostty/config
```

Reload the config from Ghostty with:

```text
cmd+shift+,
```
