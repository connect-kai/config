#!/bin/zsh

# Local shell overrides.
# This file is copied to ~/.localrc by Zsh/create-localrc.sh.
# Put machine-specific aliases, env vars, and secrets here.

unalias proxy unproxy >/dev/null 2>&1 || true

# Proton Pass SSH agent — launchd-managed, see SSH/create-config.sh
export SSH_AUTH_SOCK="$HOME/.ssh/proton-pass-agent.sock"

# Restart the agent (needed after `pass-cli login`)
alias par='launchctl kickstart -k "gui/$(id -u)/me.proton.pass.ssh-agent"'

# Proxy helpers
function proxy {
  export https_proxy="http://127.0.0.1:6152"
  export http_proxy="http://127.0.0.1:6152"
  export all_proxy="socks5://127.0.0.1:6153"
}

function unproxy {
  unset all_proxy http_proxy https_proxy
}

# Add more local-only settings below.
