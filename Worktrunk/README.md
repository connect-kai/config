# Worktrunk

## Install

```bash
bash ~/Developer/Config/Worktrunk/install.sh
```

This repository stores the Worktrunk user config in:

```text
~/.config/worktrunk/config.toml
```

The install script symlinks the config so edits in this repo are reflected immediately.
